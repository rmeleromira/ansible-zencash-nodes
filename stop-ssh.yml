---
- hosts: master
  vars_files:
    - secrets.yml
  gather_facts: false
  tasks:
    - name: Stop ssh server
      shell: lxc-attach -n {{vault_nodes[item].ansible_fqdn}} -- systemctl stop ssh
      with_items: "{{ groups['nodes'] | union(groups['monitoring'])}}"
